{
  "name": "phonebookbackend",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "shx rm -rf dist && cd ../frontend && npm run build && cp -r dist ../backend",
    "deploy:full": "shx npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "",
  "license": "",
  "description": "",
  "dependencies": {
    "express": "^5.1.0",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "shx": "^0.4.0"
  }
}

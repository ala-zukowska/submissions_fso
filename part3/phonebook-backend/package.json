{
  "name": "phonebookbackend",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "shx rm -rf dist && cd ../../part2/phonebook && npm run build && shx cp -r dist ../../part3/phonebook-backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m \"Part 3: Phonebook uibuild\" && git push"
  },
  "author": "",
  "license": "",
  "description": "",
  "dependencies": {
    "express": "^5.1.0",
    "mongoose": "^8.19.0",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "shx": "^0.4.0"
  }
}
